import styles from './spinner.module.css';

const Spinner = () => {
  return (
    <div className={styles.loader}>
      Spinning around, spinng around, move out of my way...
    </div>
  );
};

export default Spinner;
